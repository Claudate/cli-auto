# 澄清稿认领计划

## 目标
- 给谁：假设（用户跳过·直接写计划）：待写计划时补全「给谁」
- 场景：假设（用户跳过·直接写计划）：待写计划时补全「卡在哪」
- 可观察结果：假设（用户跳过·直接写计划）：待写计划时补全「做成什么样」
- 入口：我已想清，直接写计划

## 非目标 / 不做
- （待补）明确不做 / 非目标
- 说明：黄条已提醒，不拦保存与分配

## 会失去什么
- （待补）因砍范围会失去什么

## 验收
- [ ] （待补）怎样算做完
- 说明：黄条已提醒，不拦保存与分配

## 风险 / 待确认
- 假设：假设（用户跳过·直接写计划）：待写计划时补全「给谁」
- 假设：假设（用户跳过·直接写计划）：待写计划时补全「卡在哪」
- 假设：假设（用户跳过·直接写计划）：待写计划时补全「做成什么样」
- 假设：假设（用户跳过·直接写计划）：待写计划时补全「先不做」
- 假设：假设（用户跳过·直接写计划）：待写计划时补全「怎样算完」
- 假设：用户跳过澄清：直接写计划
- 假设：目标对象：假设（用户跳过·直接写计划）：待写计划时补全「给谁」
- 假设：痛苦时刻：假设（用户跳过·直接写计划）：待写计划时补全「卡在哪」
- 假设：可观察结果：假设（用户跳过·直接写计划）：待写计划时补全「做成什么样」
- 假设：明确不做：假设（用户跳过·直接写计划）：待写计划时补全「先不做」
- 假设：怎样算做完：假设（用户跳过·直接写计划）：待写计划时补全「怎样算完」

## V1 边界
- V1 只做到「假设（用户跳过·直接写计划）：待写计划时补全「做成什么样」」可演示/可分配；其余进 V2/Later

## 任务大纲
### T1 · 按已想清范围写出最小章节（目标 / 不做 / 验收）
- 说明：澄清相认领生成的 V1 步骤
- 验收：可勾选完成

### T2 · 补齐计划正文习惯章节并核对人话
- 说明：澄清相认领生成的 V1 步骤
- 验收：可勾选完成

### T3 · 保存计划草稿并进入「分配计划」
- 说明：澄清相认领生成的 V1 步骤
- 验收：可勾选完成

## V2 / Later
- （折叠）未进 V1 的愿望与扩展能力，本版不展开执行。

## 结构对齐
- 配方：R-tool · 深度 D · app-shell
- 澄清相主 CTA：认领并写成计划；后半段：分配计划
- 文件策略：Brief 仅 session 展示；认领写入 plan 草稿（无独立 brief 文件）
