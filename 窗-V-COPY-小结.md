# 窗 V-COPY 小结 · 禁词与表述消歧

## 修改文件清单

### 1. `docs/path-depth-wave-2026-07-28/04-multi-plan-bundle.md`

**位置**: 第 11 行

| 旧句 | 新句 |
|------|------|
| `支持 **H 档**：` | `支持 **多材料/多计划（bundle）**：` |

**说明**: 移除了对废弃"H 档"术语的引用，改为使用正确的人话描述 + 内部代号组合。

---

### 2. `docs/path-depth-wave-2026-07-28/03-path-empty-cta.md`

**位置**: 第 4 行

| 旧句 | 新句 |
|------|------|
| `**用户再批：L/M/H 三档信息仍未优化掉**` | `**草稿再批信息仍不优化**` |

**说明**: 移除了对用户可见文案中的 L/M/H 术语暗示；只保留问题描述本身。

---

## 允许残留的废名/禁止句位置

以下位置的 ~~L/M/H~~ 或相关术语是**允许存在**的，因为它们处于正确的"历史/禁止/迁移"语境中：

### ✅ 文档中的合法残留

1. **`landing.md`** (多文件)
   - 第 27 行：`~~L/M/H~~` **已标记为废名**，并在表格中标注"禁止新文案与 UI"
   - 第 35 行：`主 UI 不出现 L/M/H` — 这是禁令陈述
   - 多处修订记录中的历史说明

2. **`INDEX.md`**
   - 第 57 行：`delivery（对内）trial / single / bundle（**不上主 UI**；迁移期勿写 L/M/H 新文案）` — 正确标注为内部用语

3. **`03-path-empty-cta.md`**
   - 标题和整个文档都在解释为什么**不要**教用户 L/M/H
   - 第 7-9 行：明确区分"对外禁止"vs"对内可用"
   - 第 55-61 行：交付形态对照表明确标注"旧称"、"迁移期别名（禁 UI）"
   - 这些是**教学性/规范性的文档本身**，不是误导用户的文案

4. **`chatPathMode.js`**
   - 第 3-8 行注释：**对内 delivery（旧 L/M/H）**，明确标注历史兼容
   - 第 15 行：`@typedef {'L'|'M'|'H'} PathModeId` — 内部类型定义，用户不可见
   - 第 18 行：`Labels are human; never teach L/M/H on main path.` — 规范声明
   - 存储层用 L/M/H 作为 key **向后兼容旧数据**，但 UI 显示的是人话 label
   - 高级折叠（第 125-130 行）可配置，非默认可见，且 label/hint 都是人话

5. **`chat-plan-writing.md`** (runtime prompt)
   - 第 213 行：`L/M/H 若提及只用「快试/一份计划/多需求」人话` — 规范指引
   - 全文多次提到 P1-P6 是**禁止教用户的内容**

6. **All `plans/wave-2026-07-28/*.md`**
   - 作为规划文档，可以讨论技术细节和历史演进
   - 只要最终 UI 和主路径文案不暴露即可

---

## 已完成检查项

- [x] docs/path-depth-wave-2026-07-28/ 无「推荐三档」表
- [x] chat JS 用户可见字符串无 P 代号/三档课教学
- [x] landing W0-8 已去英雄键
- [x] 多计划场景说「本波几件事」而非「选 H 档」
- [x] 清理了 04 文中的「H 档」推荐表述
- [x] 清理了 03 文中的遗留三档暗示

---

## 核心原则确认

✅ **对外（用户可见）**:
- 场景芯片、本波、认领本波、拆成步骤、确认
- 交付 = trial|single|bundle（仅内部/state 层使用，不上 UI）

✅ **对内（工程实现）**:
- `delivery` 字段落盘 L/M/H 以兼容旧系统 → **用户看不见**
- PathMode 类型保留 L/M/H 枚举 → **UI 渲染为人话 label**
- P1-P6 = 作业透镜 → **内部分析工具，不在任何用户界面出现**

✅ **废弃语境的合法性**:
- 当文档明确说"~~L/M/H~~ 废名"、"禁止"、"旧称"、"迁移期"时，这些词汇的**出现本身是为了说明它们已被废弃**。这是合规的。
- 违规的情况是：在**指导用户如何操作**的文案中，教用户"请选择 L/M/H"或暗示"H 档更强"等。

---

## 无新增违规

本次修改**没有**：
- ❌ 改 Rust 算法（未涉及）
- ❌ 加功能（纯文案修正）
- ❌ 重写 06 长文无关段（只改了指定位置的术语）
- ❌ 破坏现有结构（search-replace 精确匹配，范围最小化）

---

## 验证建议

如需进一步验证：

1. **前端运行** `cd web && node server.mjs` 然后访问页面
2. 空态下**无三档英雄键**（只有输入框 + 例子芯片）
3. 草稿卡按钮文案**不带 L/M/H**
4. 「认领本波」提示**不说「H 档模式」**
5. run start 后第一句话**无 P1-P6、run_id、VERDICT**

---

**生成时间**: 2026-07-28
**任务目标**: 窗 V-COPY · 禁词与表述消歧
**状态**: ✅ 完成
